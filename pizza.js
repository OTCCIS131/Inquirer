var inquirer = require('inquirer');
const chalk = require('chalk')

console.log(chalk.red('Order your pizza here'))

class Pizza{
    constructor(size, cheese, sauce, toppings, cut){
        this.size=(asize)
        this.cheese=(extra)
        this.sauce=(asauce)
        this.toppings=(atoppings)
        this.cut=(acut)
    }
}
inquirer.prompt(questions) > promise